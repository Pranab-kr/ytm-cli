//! ratatui widgets. Each one draws into a `Rect` and reads `AppState`; none of
//! them mutate it.
pub mod nowplaying;
pub mod playlists;
pub mod queue;
pub mod search;
pub mod sidebar;
pub mod tracklist;
